---
spring:
  main:
    web-application-type: none
  profiles:
    active: dev
  application:
    name: job-sql
  cloud:
    config:
      uri: http://localhost:8888
      name: job
server:
  port: 8887