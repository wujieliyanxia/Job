---
spring:
  main:
    web-application-type: SERVLET
  profiles: dev
  application:
    name: job-api
  cloud:
    config:
      uri: http://localhost:8888
      name: job
      profile: dev
server:
  port: 8887


---
spring:
  main:
    web-application-type: SERVLET
  profiles: test
  application:
    name: job-api
  cloud:
    config:
      uri: http://120.53.7.165:8888
      name: job
      profile: test
server:
  port: 8887