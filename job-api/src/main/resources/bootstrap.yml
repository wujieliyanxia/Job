---
spring:
  main:
    web-application-type: SERVLET
  profiles:
    active: dev
  application:
    name: job-api
  cloud:
    config:
      uri: http://localhost:8888
      name: job
server:
  port: 8887